interface:
  display_name: "Smart Video"
  short_description: "Create and render editable Jogg videos locally"
  default_prompt: "Use $smart-video to create a 10-minute Chinese video with the avatar only in the opening and closing shots."

policy:
  allow_implicit_invocation: true
