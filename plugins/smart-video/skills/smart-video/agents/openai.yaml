interface:
  display_name: "Smart Video"
  short_description: "Create and render editable Jogg videos locally"
  default_prompt: "Use $smart-video to create a 90-second video from my topic or sources and render it locally."

policy:
  allow_implicit_invocation: true
